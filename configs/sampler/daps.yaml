name: unified_latent

num_iters: 50

schedule: timestep
rho_min: null
rho_max: null

method: hmc
sample_num_iters: 10
sample_learning_rate: 1e-5
sample_momentum: 0.45

num_burn_in_iters: 40
num_samples_per_run: 20
